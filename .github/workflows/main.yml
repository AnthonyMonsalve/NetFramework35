name: Acción de Autohospedado
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Hacer git clone
        run: |
          cd C:\Users\Administrador\Documents
          $fecha = Get-Date
          $fechaString = $fecha.ToString()
          $fechafinal = $fechaString.Replace(" ", "-").Replace(":", "-").Replace("/","-")
          mkdir $fechafinal
          cd $fechafinal
          git clone https://github.com/AnthonyMonsalve/NetFramework35.git
          cd NetFramework35
          msbuild /t:Build /p:Configuracion=Release
          $directory = $PWD.path/bin
          Move-Item -Path "$directory" -Destination "C:\inetpub\wwwroot\TestIIS" -Force -Recurse
